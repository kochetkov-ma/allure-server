# DEPRECATED -- tasks moved

This directory is no longer used for task tracking. The canonical task board lives at:

**`.claude/features/board.md`** (procedure: `.claude/features/TRACKER.md`)

Do not write tasks, state, or logs here. Previous contents (brewcode plugin version cache
+ session logs only -- no task data) were removed on 2026-06-11 during the migration to
the file-based board.

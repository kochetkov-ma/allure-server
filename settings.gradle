plugins {
    // Resolves the Java 25 toolchain declared in build.gradle, downloading it when the
    // host has no matching JDK. Must be applied in settings, before any project is built.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'allure-server'
